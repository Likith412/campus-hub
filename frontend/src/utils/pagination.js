// Page-size choices offered by every paginated list. 50 is the backend's max limit.
export const PAGE_SIZE_OPTIONS = [10, 20, 50];
// Smaller ladder for the compact side panels (club notices, event roster, profile events).
export const PANEL_PAGE_SIZE_OPTIONS = [5, 10, 25];
